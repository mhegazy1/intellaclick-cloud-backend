# Server Configuration
NODE_ENV=production
PORT=5000

# Database
MONGODB_URI=mongodb://localhost:27017/intellaclick

# JWT Secret (generate a strong random string)
JWT_SECRET=your-super-secret-jwt-key-here

# CORS Origins (comma-separated)
# Include your Netlify domain and any custom domains
# Netlify patterns like *.netlify.app are automatically allowed in production
ALLOWED_ORIGINS=https://intellaclick.netlify.app,https://intellaclick.com,https://www.intellaclick.com

# Email Configuration
# For production, use a dedicated email service like SendGrid, AWS SES, or Mailgun
# For Gmail: Enable 2FA and create an App Password at https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password-or-api-key
EMAIL_FROM=IntellaClick <noreply@intellaclick.com>

# Portal URLs (used in email links for verification and password reset)
STUDENT_PORTAL_URL=https://join.intellaclick.com
INSTRUCTOR_PORTAL_URL=https://instructor.intellaclick.com

# Legacy Frontend URLs (kept for backward compatibility)
FRONTEND_URL=https://intellaclick.com
DESKTOP_APP_DOWNLOAD_URL=https://intellaclick.com/download

# Optional: Stripe for payments
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Optional: S3 for file storage
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
S3_BUCKET_NAME=